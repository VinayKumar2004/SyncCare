# Frontend Module Layout

- `app/`: app bootstrap and global providers
- `features/`: feature-first modules (auth, pairing, reminders, chat, playback)
- `shared/`: reusable cross-feature components, hooks, services, and storage adapters
